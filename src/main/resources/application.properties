database.ip = ${DB_IP:ec2-54-228-218-84.eu-west-1.compute.amazonaws.com}
database.port = ${DB_PORT:5432}
database.user = ${DB_USER:wkgravnujyofky}
database.password = ${DB_PASSWORD:b40f60ded972bd0700a8aeafd60243b51c13f00cb23026ef5d4a31eb7e45a2be}
database.name = ${DB_DBNAME:d4hpartfrcgrmu}

#database.ip = ${DB_IP:localhost}
#database.port = ${DB_PORT:5432}
#database.user = ${DB_USER:postgres}
#database.password = ${DB_PASSWORD:root}
#database.name = ${DB_DBNAME:licenta}

spring.datasource.url = jdbc:postgresql://${database.ip}:${database.port}/${database.name}
spring.datasource.username = ${database.user}
spring.datasource.password = ${database.password}
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto = update
server.port=${PORT:8080}

